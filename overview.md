# 《仕途履途》文字模拟抉择游戏 — 交付说明

单页 H5 文字游戏，零依赖、零构建，双击 `index.html` 即玩（手机浏览器同样适配）。

## 一、文件结构

| 文件 | 内容 |
|---|---|
| `index.html` | 四栏骨架：顶栏 / 左侧面板 / 中央主区 / 底栏 |
| `css/style.css` | 墨青公文配色系统、纸质卡片、弹窗、移动端抽屉 |
| `js/data.js` | 常量、四背景、职级序列（10 级）、隐患四级、特殊事件、18 套结局 |
| `js/posts.js` | 37 个岗位（30 常规 + 4 副国 + 3 正国）+ 90 个岗位专属事件 |
| `js/events.js` | 12 类日常事件、6 类国级专属事件、隐患爆发文本、季度空档提示 |
| `js/engine.js` | 季度推进、年末八步结算、晋升、隐患、国级校验、结局评定、存档 |
| `js/ui.js` | 全部渲染与交互 |
| `js/main.js` | 启动与按钮绑定 |
| `balance_check.js` | 国级模块回归验证脚本（Node 无头，详见第七部分） |

## 二、已实现机制

**岗位体系**：乡镇 / 城郊乡镇 / 县直 / 市直 / 省直 五层级 × 6 岗位 = 30 个。每个岗位只展示「名称 + 职责」，后台挂 6 个隐藏权重（三类政绩系数、风险、借调概率、职数松紧）。文秘、保障、编审类岗位职数极紧（明升暗降），城郊征迁、项目类岗位职数松、攻坚系数高（明降暗升）。

**晋升选择**：晋升成功时随机生成 2–3 个同职级岗位，只列职责，不列优劣，玩家自行判断。

**潜伏隐患**：高危选项按等级以 35%–75% 概率埋雷，最多同时携带 3 个。到期后先做「自然消解」判定（1 级 55%、2 级 40%、3 级 20%、4 级 5% 就此过去），未消解才爆发。四级后果：谈话提醒 → 瑕疵通报（考核降档）→ 冻结晋升 1–2 年 → 追责终止。全程界面零提示。

**年末八步结算**（严格按序）：时间更迭 → 试用期判定 → 乡镇最低服务期 → 四档考核 → 职级晋升（含多岗位选择）→ 纪律隐患检查 → 随机特殊事件（借调 / 轮岗 / 遴选 / 考察）→ 退休终判。

**日志履历**：11 类日志，左侧只显示最近 3 条，完整时间轴可查；埋雷只记录行为、不记录风险，爆发后才记录瑕疵。

**存档**：3 个槽位，localStorage，中途退出结算也能原样恢复。

## 三、平衡验证结果

用 600 局无头模拟 + 120 局完整 DOM 交互冒烟（模拟真实点击）验证：

- 平均生涯 33.8 年，经历 6.2 个岗位，隐患爆发 2.1 次/局
- 最终职级分布：正厅 3%、副厅 19%、正处 22%、副处 21%、正科 21%、副科 10%、科员 2%
- 14 套结局全部可达，保守玩法偏向「宏观高阶 / 极致审慎」，冒险玩法偏向「隐患遗留 / 追责终止」
- 无死循环、无运行时异常，存档往返一致

## 四、国级模块（副国级 / 正国级）

在**完全保留**原有 UI、30 岗位、事件、隐患、年末八步、日志、1–14 结局的前提下扩展：

**职级序列延伸**：科员→…→正厅（rank 0–7）之上新增 **副国级（rank 8）/ 正国级（rank 9）**，共 10 级。

**奇迹级晋升路径（严禁跳级，逐核验）**：副国必须由「正厅级核心主官岗」晋升，且须**同时满足全部前置条件**：
1. 现任正厅级核心主官岗位
2. 基层 / 县域 / 地市 / 省直四段主官履历齐全
3. 整个生涯零隐患埋雷、零隐患爆发、无在途隐患
4. 多次「优秀」考核 + 多次「组织考察」储备
5. 触发罕见【高阶专项组织考察】随机事件（副部级及以上方可能，约 12%/年）

即便全部条件达成，晋升副国触发概率仍极低（约 5%）；正国门槛更高、概率更低（约 1.4%）。正国之上不再晋升，仅平调/退休。

**国级专属内容**：4 个副国岗位 + 3 个正国岗位（界面只显名称+职责，不暴露任何后台权重/概率）；6 类国级专属季度事件（低职级永不刷出，部分选项暗藏更高等级隐患）；4 套新结局（E15 副国高位成就型 / E16 正国顶峰圆满型 / E17 副国高位陨落型 / E18 正国高位陨落型）。国级阶段隐患破坏力上调一级，更易触发高位问责终止——**即便登顶也非「通关」**。

## 五、国级平衡验证（`balance_check.js`）

Node 无头运行，覆盖回归对比 + 定向条件测试 + 4900 局模拟：

- 低职级（rank 1–7）晋升池国级岗位泄漏：**0**（通过）
- 条件全满足时晋升成功率：**副国 5.2% / 正国 1.4%**（极低，符合 spec）
- 10 项前置条件逐项拦截：全部正确拦截（缺任一段履历 / 有隐患 / 优秀不足 / 考察不足 / 未触发高阶考察 / 非核心主官 均被挡下）
- 严禁跳级：数千次晋升越级次数 **0**（通过）
- 900 局混合玩法：国级达成 **0%**（普通玩法几乎不可及），1–14 结局齐全
- 4000 局最优谨慎玩法（全程审慎 + 贪心补齐四段主官履历）：**副国 0.47%**、正国 0%，E15×15 / E17×4 正常出现

**结论**：副国/正国确为「奇迹级」成就——普通玩法触及概率近零，连最优谨慎玩法也仅约 0.5% 触及副国，正国更罕。这正契合 spec「无法通过堆政绩获得、触发概率极低」的设计意图；同时机制已验证可达、前置门槛全部生效。

## 六、硬性规范遵守情况

- 界面不出现任何后台系数、风险值、隐患值、借调概率、职数松紧
- 所有选项按钮视觉完全一致，无对错颜色提示
- 实绩概览改用定性词（「初有起色」「较为扎实」），不暴露数字
- 无渐变、无发光、无卡通元素，思源宋体 + 6px 圆角纸质卡片

## 七、已知限制

- 存档存于浏览器本地，换浏览器或清缓存会丢失
- 一局约 100 次抉择，完整通关需较长时间，随时可存档中断
- 真实世界国级岗位名已泛化为职能名（如「国家政务统筹岗」），职责文本保留设定原意

## 八、季度‑年度时序修正（已验证）

**修复的严重 BUG**：原代码 `S.quarter` 为 1-based，且 `onNext`/`renderStage` 在 `quarter >= 3`（第三季度）就触发年末结算，并硬编码 `S.quarter = 3`，导致一年只跑 Q2、Q3 两个事件即结算，且首事件跳过 Q1、缺 Q4 事件——违反 spec「固定 4 季度、仅 Q4 后结算」。

**修正方案**（全部保留事件/结算/隐患/晋升/结局逻辑不变）：
- 改为 0-based 季度模型：`newGame` 与 `enterNextYear` 的 `quarter` 初值/重置由 `1` 改为 `0`
- 结算触发阈值 `>= 3` → `>= 4`（覆盖 `renderStage` / `updateBot` / `onNext` 三处）
- 删除 `onNext` 与「开始年度结算」按钮里硬编码的 `S.quarter = 3`，避免破坏季度计数

**验证**：`smoke_check.js` 用最小 DOM 桩驱动真实 `UI.onNext` 流程，连跑 37 年——**每年恰好 4 个季度事件、0 次提前/错季结算、结算只在 quarter===4 触发**。`balance_check.js` 同步改为 `quarter < 4`，逻辑校验全过。

**平衡影响（时序修正的固有后果，非逻辑改动）**：每季事件由 2–3 个增至 4 个，年度政绩累积变多→考核更易「优秀」→晋升明显变易。现状：混合玩法正厅级升至 ~40%（原 ~26%），理想玩法副国达成率 0.47%→1.30%、正厅级 95.6%。副国/正国仍为奇迹级（混合触及率 <0.2%）。如需恢复「正厅稀罕」的旧难度，可单独微调 `PROMO_BASE`/事件效果量级，属平衡调参、不影响本时序修正。

## 九、自愿问题反馈面板（新增，未改原有逻辑/UI/时序/事件）

底部功能栏新增【问题与反馈】按钮，与「下一季度 / 存档 / 读档 / 完整日志」横向并列，任何游玩阶段（含退休/追责结局页）均可点击。点击弹出 6px 圆角墨青弹窗（复用既有 `.modal` 骨架，配色零新增）：

- 标题「游戏问题与体验反馈」+ 说明文字（纯本地、不上传）
- 问题类型下拉：时序/季度结算异常、晋升/岗位逻辑错误、事件文案异常、潜伏隐患机制异常、结局匹配错误、UI显示问题、其他体验建议（共 7 类）
- 大文本输入框（现象描述/复现步骤）
- 【生成反馈文本】→ 在弹窗内输出结构化文本（问题类型 + 描述 + 当前游戏快照：年龄/工龄/职级/岗位/所处季度 + 年份）；【关闭】关闭弹窗
- 生成后附【复制反馈文本】按钮，复制失败自动降级为「选中文本长按复制」

约束全部满足：无后端/不上传、不自动弹出、关闭不干扰游玩、结局页仍保留入口、UI 风格统一。

**验证**：`node --check` 全过；DOM 桩驱动 `showFeedback→生成`——弹窗元素齐全、空描述被拦截、结构化文本含完整快照、结局页仍可生成；`smoke_check.js` 时序回归 0 违规（原有逻辑无回归）。临时测试脚本已清理。

## 十、底部按钮栏重复渲染修复（新增防御性重建）

针对用户反馈的底部按钮栏重复渲染（问题与反馈/存档/读档/完整日志 出现多余副本），新增防御性修复：

- `js/main.js` 新增 `ensureBotbar()`（在绑定阶段最先调用），以单一配置数组 `BOTBAR` 为准：
  1. 先剔除 `#botbar` 下「非规范 id 的注入副本」与「同名 id 的重复按钮」；
  2. 再确保 5 个规范按钮存在、文案/样式/`onclick` 正确、按序横向排列。
- `index.html` 中 `btnNext` 静态文案「下一季度」→「推进季度」，与规范列表一致。
- 正确按钮列表（正好 5 个）：【推进季度】【存档】【读档】【完整日志】【问题与反馈】。

约束全部满足：每个按钮只生成 1 个、无重复副本、每个都正确绑定点击事件（无幽灵按钮）、横向 `flex` 均匀排列且样式统一、原有业务逻辑与全部弹窗（含反馈面板）完整保留。

**验证**：`botbar_check.js`（功能型 DOM 桩，预置 9 按钮脏 DOM：同名重复 + 异名副本 + 幽灵按钮）加载 main.js 后，断言 botbar 修复为正好 5 个、顺序正确、各按钮 `onclick` 正确触发对应 `UI.*`、副本与幽灵全部清除；`smoke_check.js` 时序回归 0 违规。临时测试脚本已清理。

### 二次加固（彻底修复版）
按用户进一步要求，改为"渲染最开始先清空再重建"：
- `ensureBotbar()` 第一步 `while (bar.children.length) bar.removeChild(bar.children[0]);` 彻底清空 `#botbar` 全部旧节点，再 `forEach` 全新生成固定 5 个按钮（顺序严格：推进季度→存档→读档→完整日志→问题与反馈，每个仅 1 份、文案不重复、均绑定对应点击）。
- 验证：`botbar_check.js` 用 9 按钮脏 DOM（含同名重复+异名副本+幽灵）加载 main.js 后，修复为正好 5 个、顺序严格、文案无重复、各 `onclick` 正确触发对应 `UI.*`、零残留；`smoke_check.js` 时序 0 违规。临时脚本已清理。

## 十一、移动端适配微调（仅新增）
- `index.html`：`<head>` 最顶部新增 `<meta name="viewport" content="width=device-width, initial-scale=1.0">`。
- `css/style.css` 末尾新增 `@media (max-width:768px)`：body 14px、底部栏 `#botbar` 自动换行、`.bb-btn` 间距字号、`.modal` 限宽 92vw。通用类 `.btn-group` 已替换为实际类 `#botbar` / `.bb-btn`。
- **特殊事件选项显示 undefined 修复**：根因是两套事件数据字段名不一致——普通/岗位事件用 `x/f/h/lg`，`SPECIALS` 用 `text/fx/hazard/log`，而 `engine.js` 与 `ui.js` 只按前者读取。修复：`engine.js` 映射处归一为 `x: o.text||o.x`、`_opt` 输出 `f:o.fx||o.f`/`h:o.hazard||o.h`；`ui.js` 新增 `optText()` 兜底（空值显示「（选项加载异常）」）并用于 `showEvent` 与 special 两处渲染。**副作用（✅ 用户已确认保留，本模块定稿）**：借调/轮岗/遴选/考察的属性加成与隐患埋雷此前一直空转，现已真正生效，会影响晋升/隐患节奏。最终确认：`_opt.f` / `_opt.h` 映射不移除；选项 undefined 修复、全局兜底、普通事件渲染均保持现状，无需再调整。

> 本模块（特殊事件选项渲染与属性生效）已于 2026-09-09 定稿归档。后续如需回退"特殊事件不改属性"的旧手感，去掉 `_opt.f` / `_opt.h` 两行映射即可。
- 反馈弹窗（最终版）：`main.js` 的 `openFeedback()` 重写为「自动复制游戏状态快照 + 弹窗引导」，两按钮【前往填写反馈表单】（新标签打开 `https://f.wps.cn/g/OKPujzAG/`）与【取消】（关闭弹窗留在游戏）。弹窗全部内联样式，不污染原 CSS。
  - 复制提示改为弹窗内内嵌：移除 `alert`（仅 console 日志），弹窗内 `<p id="feedbackTip">` 按结果显示——成功「✅ 已复制到剪贴板」；失败「⚠️ 当前环境无法自动复制快照…」红色内联样式（不动外部 CSS）。因剪贴板 API 异步，采用「同步可用性定初始文案 + `.catch` 时再改为红色提示」双保险，file:// 与 https 文案均准确。
  - 快照内容已改为真实游戏状态：年龄 / 工龄 / 当前职级 / 当前岗位 / 所处年份 / 所处季度(Q1–Q4) / 版本 / 时间戳 / UA，全部读取项目已有全局 `Engine`（`S.age/seniority/year/quarter`、`curRank()`、`curPost()`），逐层 `typeof` 兜底、取不到显示"未知"。季度需注意：`S.quarter` 为 0-based，且事件/结果界面时已自增（用 `UI.mode` 区分），否则事件里会多算一季度。
  - 关键兜底：① `getCurrentLevelName?.()`/`getSaveStatus?.()` 这两个全局函数本项目并不存在，可选链对未声明标识符无效会抛 `ReferenceError` 中断弹窗，改用 `typeof x === 'function'` 判断；② `file://` 下 `navigator.clipboard` 常为 `undefined`，`.writeText` 会抛同步 TypeError（`.catch` 接不住），需先判存在再 `try/catch`。修后复制失败也不阻断弹窗。
- 反馈入口改为在线表单：`js/main.js` 新增 `openFeedback()`，底部【问题与反馈】按钮点击后新窗口打开 WPS 表单 `https://f.wps.cn/g/OKPujzAG/`。本地反馈面板 `UI.showFeedback`（生成结构化文本+游戏快照）代码保留未删，仅不再由该按钮触发；底部栏仍为严格 5 个按钮，其余按钮绑定不变。
- 底部按钮样式细化：容器 `#botbar`（flex 换行居中 + 内边距 + `height:auto;min-height:var(--bbh)`），按钮 `#botbar .bb-btn`（`white-space:nowrap` 防中文断字、13px、`min-width:90px/max-width:140px`）。随后 `--bbh` 由 56px 调为 66px，使 `#app`/`#sidePanel`/`#toastRoot` 预留空间与加高后的底栏对齐，换行不再被遮挡。
- 后续按用户要求合并 viewport：删除全部旧 viewport meta，仅保留顶部一条合并完整版 `<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1, user-scalable=no, viewport-fit=cover">`（置于 `<head>` 首行）。现仅 1 条 viewport，刘海屏安全区等属性生效，移动端 CSS 与游戏逻辑均未改动。
